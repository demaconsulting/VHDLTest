---
sections:
  - title: VHDLTest Requirements
    sections:
      - title: SelfTest Requirements
        requirements:
          - id: VHDLTest-SelfTest-Run
            title: VHDLTest shall support self-validation mode.
            justification: |
              Enables users to verify that VHDLTest is correctly installed and configured in
              their environment. This diagnostic capability helps identify configuration issues
              before running actual test suites, reducing troubleshooting time.
            tests:
              - SelfTest_Validate_MockSimulator_ReturnsSuccess
            children:
              - VHDLTest-SelfTest-Validation-Run

          - id: VHDLTest-SelfTest-Depth
            title: VHDLTest shall support configurable validation report depth.
            justification: |
              Enables control of markdown header depth in validation reports when embedding the
              validation output in other documents. This ensures proper document hierarchy and
              formatting when validation reports are included as sections in larger documents.
            tests:
              - SelfTest_ValidateWithDepth_MockSimulator_RendersDepthHeadings
            children:
              - VHDLTest-SelfTest-Validation-Depth

          - id: VHDLTest-SelfTest-Results
            title: VHDLTest shall save validation results to file.
            justification: |
              Enables persistent storage of validation results for documentation, compliance,
              and historical comparison purposes. Saved reports can be shared with team members
              or included in bug reports for support.
            tests:
              - SelfTest_ValidateWithResultsFile_MockSimulator_SavesResults
            children:
              - VHDLTest-SelfTest-Validation-Results

          - id: VHDLTest-SelfTest-SystemInfo
            title: VHDLTest shall include system information in validation reports.
            justification: |
              Provides essential context for validation results by capturing the execution
              environment (OS version, runtime, etc.). This information is crucial for
              reproducing issues and diagnosing environment-specific problems.
            tests:
              - SelfTest_Validate_MockSimulator_IncludesOSVersionInReport
            children:
              - VHDLTest-SelfTest-Validation-SystemInfo

          - id: VHDLTest-SelfTest-InvalidSimulator-ExitCode
            title: VHDLTest shall exit with a non-zero code when given an unrecognized simulator name.
            justification: |
              If an unrecognized simulator name is provided, VHDLTest should fail gracefully
              rather than throwing an unhandled exception, ensuring a good user experience
              when the configuration is incorrect.
            tests:
              - SelfTest_Validate_InvalidSimulator_ReturnsFailure
            children:
              - VHDLTest-SelfTest-Validation-InvalidSimulator-ExitCode

          - id: VHDLTest-SelfTest-InvalidSimulator-ErrorMessage
            title: VHDLTest shall report a descriptive error message when given an unrecognized simulator name.
            justification: |
              A descriptive error message when an unrecognized simulator name is provided
              helps the user understand what went wrong and how to correct the configuration,
              rather than leaving them with a bare non-zero exit code.
            tests:
              - SelfTest_Validate_InvalidSimulator_ReturnsFailure
            children:
              - VHDLTest-SelfTest-Validation-InvalidSimulator-ErrorMessage
